/**
 * Placeholder component representing the current toast.
 *
 * @returns `null` — no rendered output
 */
export function CurrentToast() {
  return null;
}

/**
 * Placeholder component for toast control UI; intentionally renders no output.
 *
 * @returns `null` (no UI is rendered)
 */
export function ToastControl() {
  return null;
}